import 'package:flutter/material.dart';

class settingsfoodapp extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Settings')
        ),
 body: Column(
   children: [
     Center(child: Text('Settings'))
   ],

 ),

    );
  }
}
